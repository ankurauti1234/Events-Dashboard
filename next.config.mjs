/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "apm-logo-bucket.s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
